/**
 * `@killertux/js-slim` — a typed SLiM protocol harness for FitNesse.
 *
 * This module is the public entry point. It is intentionally minimal at the
 * scaffold stage; protocol, runtime and transport APIs are added in later steps.
 */

/** The package version, kept in sync with `package.json`. */
export const VERSION = "0.1.0";
